# The production Compose file loads additional Project Zomboid settings from
# .env. The variable names were not present in the source Compose file supplied
# for sanitization, so no values are guessed here.
#
# Add only the required variable names and safe placeholder values before
# publishing a more complete example.
